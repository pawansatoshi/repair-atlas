[project]
name = "repair-atlas-agent"
version = "0.1.0"
description = "RepairAtlas AgentCore runtime"
requires-python = ">=3.12"
dependencies = [
  "bedrock-agentcore",
  "boto3",
  "psycopg[binary]",
]

[tool.uv]
package = false
